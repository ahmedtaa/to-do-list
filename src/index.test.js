// describe for delete;
// describe for create;

const { deleteTask, createTask } = require('./index.js');

describe('deleteTask', () => {
  test('should ', () => {
    expect(deleteTask('hello')).toBe(); // TODO
  });
});

describe('createTask', () => {
  test('should', () => {
    expect(createTask()).toBe();// TODO
  });
});
